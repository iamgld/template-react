/* eslint-disable no-unused-vars */
export enum PublicRoutes {
	auth = 'auth',
	login = 'login',
	register = 'register',
	recover = 'recover',
	wrong = 'wrong',
}

export enum PrivateRoutes {
	dashboard = 'dashboard',
	home = 'home',
	users = 'users',
	wrong = 'wrong',
}
